## 1. Establish Service and Backend Foundations

- [x] 1.1 Map existing service code and runtime configuration to `services/product/` or `services/platform/` before moving files.
- [x] 1.2 Move backend, LLM, TTS, and avatar ownership into `services/product/{backend_service,llm_service,tts_service,avatar_service}` with concise packages under each `src/` directory and independent `pyproject.toml` plus `uv.lock` files.
- [x] 1.3 Move LiveKit, LMCache, Postgres, and Redis runtime configuration into `services/platform/{livekit,lmcache,postgres,redis}` without vendoring upstream source or adding product application packages.
- [ ] 1.4 Update imports, repository-root build contexts, Dockerfile-specific ignore paths, scripts, locks, workflow paths, documentation, and Terraform references to the canonical service paths.
- [ ] 1.5 Verify each product service build and each platform runtime's real configuration, image pin, readiness, and smoke behavior after the path migration.
- [ ] 1.6 Add `observability/context.py` and `observability/logging/{__init__,config,setup,filters,formatter,daily_handler,active_session_handler}.py` to each product service, including validated transport propagation, idempotent setup, and guaranteed context cleanup.
- [ ] 1.7 Implement `active_session_handler.py` under each product service and platform log collection under `{product,platform}/<service>.log`; truncate at session start, retain after completion, and create no file keyed by `session_id`.
- [ ] 1.8 Implement daily per-service UTC rotation and `LOG_RETENTION_DAYS` cleanup under `{product,platform}/<service>/YYYY-MM-DD.log`.
- [ ] 1.9 Implement aligned four-level logfmt with level width 7 and service width 8, quoted whitespace values, TTY-only console colors, file output without ANSI escapes, approved structured fields, and sensitive-key redaction/omission.
- [ ] 1.10 Test concurrent context isolation, transport propagation, idempotent setup, invalid-level startup failure, active-session overwrite, daily retention, alignment, quoted values, TTY/file separation, and secret-free output.
- [ ] 1.11 Move Director under `backend/application/director/` and move its prompt constants from Python configuration into `application/director/prompts/{base_sales_vi,director_decision_vi,response_guardrails_vi,fallback_response_vi}.md`.
- [ ] 1.12 Implement the fixed-file prompt loader with startup validation, static-content caching, and rejection of arbitrary paths.
- [ ] 1.13 Compose decision and fallback flows with non-overridable guardrails and clearly delimited untrusted runtime context.
- [ ] 1.14 Test composition order, missing files, invalid paths, fallback selection, prompt hashing, and absence of rendered prompt or customer data in logs.
- [ ] 1.15 Add the minimal `backend/main.py` entrypoint and `backend/bootstrap/{__init__,app_factory,container,lifespan}.py` composition package.
- [ ] 1.16 Replace the mutable `api/v1.py` dependency singleton with a lightweight typed bootstrap container and FastAPI dependency access through application state.
- [ ] 1.17 Move database connection, shared resource initialization, and bounded coordinator/publisher/client/render shutdown into the bootstrap lifespan and test startup, failure, cancellation, and cleanup behavior.
- [ ] 1.18 Implement `api/middleware/{access_log,body_limit,security_headers}.py`, register framework CORS in `bootstrap/app_factory.py`, and implement `api/security/{authentication,authorization,rate_limit}.py` with shared dependencies and exception handlers directly under `api/`.
- [ ] 1.19 Test safe access fields and guaranteed context cleanup; fixed-length and chunked body `413` rejection before route execution; framework CORS; security headers; constant-time credential checks; `401` versus `403`; REST/WS/session rate limits; and WebSocket rejection before `accept()`.
- [ ] 1.20 Add backend `api/health.py` with dependency-free `/health/live` and configured-dependency `/health/ready`; split `api/v1.py` into `api/v1/router.py` plus exact sessions, avatars, voices, admin, and WebSocket route/schema modules; exclude operational health from the v1 contract and add no versioned health, media, engines, control, LLM, or TTS production route.
- [ ] 1.21 Implement `application/{sessions,playback_worker,playback_queue,text_chunker}.py` and decompose `application/director/` into config, coordinator, session context, state, decision, decision preparation, comment buffer, clustering, embeddings, scoring, routing, catalog, hooks, events, and prompts; test session lifecycle, decision parity, event/persistence delegation, LLM-to-avatar orchestration, streaming boundaries, backpressure, cancellation, and absence of empty avatar/voice service wrappers.
- [ ] 1.22 Move thin outbound transports into `application/clients/llm/openai_compatible.py`, `application/clients/tts/{self_hosted,elevenlabs,openai_speech}.py`, `application/clients/avatar/{self_hosted,liveavatar,baidu_xiling}.py`, and `application/clients/livekit.py`; enforce server-side authentication, serialization, bounded timeout/retry, response parsing, and typed transport errors without model-engine code.
- [ ] 1.23 Move internal run-plan and utterance types into `application/schemas/`; consolidate the session-store contract, memory/Redis/Postgres adapters, and raw runtime SQL under `db/` and `db/sql/runtime_schema.sql`; update imports and test DTO mapping plus all configured stores.
- [ ] 1.24 Keep vLLM/SGLang/Transformers and LMCache integration in `llm_service`, VieNeu/CosyVoice in `tts_service`, and AvatarForcing, avatar sessions, and LiveKit publishing in `avatar_service`; move hosted-provider protocols to backend clients and verify browser media bypasses backend.
- [ ] 1.25 Migrate workbench callers from `/lite/*` to the canonical session API and ensure debug, mock, sandbox-test, media, engine, control, LLM, and TTS routes are absent from the production application before removing legacy mounts.
- [ ] 1.26 Update ASGI launch commands, containers, scripts, tests, and documentation to `backend.main:app`, then remove the superseded `server.py`.
- [ ] 1.27 Verify app-factory isolation, lifespan cleanup, dependency direction, avatar/voice facades, `/health/live` and `/health/ready` semantics, both WebSocket handler roles, health exclusion from v1 OpenAPI, deterministic service-owned schema/OpenAPI generation, canonical route inventory, stable error envelopes, client failure mapping, and absence of prohibited production routes.
- [ ] 1.28 Create the complete `llm_service/src/llm` layout with minimal `main.py`, `config.py`, `bootstrap/{app_factory,lifespan}.py`, `api/health.py` live/ready endpoints, versioned chat/model routes and schemas, explicit middleware/security/error handling, self-host engine package, the full observability package, `contracts/v1/openapi.json`, and unit/integration/contract tests.
- [ ] 1.29 Create the complete `tts_service/src/tts` layout with minimal `main.py`, `config.py`, `bootstrap/{app_factory,lifespan}.py`, `api/health.py` live/ready endpoints, versioned speech/voice routes and schemas, explicit middleware/security/error handling, self-host engine package, the full observability package, `contracts/v1/openapi.json`, and unit/integration/contract tests.
- [ ] 1.30 Create the complete `avatar_service/src/avatar` layout with minimal `main.py`, `config.py`, `sessions.py`, `bootstrap/{app_factory,container,lifespan}.py`, `api/health.py` live/ready endpoints, versioned avatar/session routes and schemas, explicit middleware/security/error handling, AvatarForcing engine, LiveKit publishing, the full observability package, `contracts/v1/openapi.json`, and unit/integration/contract tests.
- [ ] 1.31 Remove redundant `inference.py`, `synthesis.py`, and `rendering.py` delegation layers; route LLM/TTS requests through dependencies to `engines/base.py`, and route avatar lifecycle through `sessions.py` to its engine and publisher.
- [ ] 1.32 Migrate LiveAvatar, Baidu Xiling, ElevenLabs, OpenAI Speech, and other hosted-provider clients from service or legacy provider locations into backend outbound client packages; keep provider credentials and provider session tokens server-side and expose only authorized browser-safe LiveKit connection data.
- [ ] 1.33 Migrate configuration to the exact `LLM_ENGINE`, `TTS_ENGINE`, `AVATAR_ENGINE`, `LLM_ADAPTER`, `TTS_ADAPTER`, `AVATAR_ADAPTER`, and matching `*_BASE_URL` vocabulary; fail startup for ambiguous legacy engine selectors such as `openai_compat`, `remote_http`, and `remote_avatar`.
- [ ] 1.34 Make deployment desired counts conditional on selected adapters so a hosted-provider path leaves the corresponding self-host service undeployed or at zero; verify service authentication, authorization scopes, body limits, GPU-aware request/concurrency limits, lifecycle cleanup, direct provider calls, contract compatibility, secret isolation, and the zero-cost idle topology.
- [ ] 1.35 Create the complete `services/platform/` layout with `README.md`; local/sandbox LiveKit Docker/config/entrypoint/validation/smoke assets; LMCache Docker/config/validation/smoke/benchmark assets; Postgres documentation/smoke assets; and Redis documentation, local config, and smoke assets, with no `src/`, product API, or vendored upstream source.
- [ ] 1.36 Replace the floating local LiveKit base image with an approved immutable version and digest; remove ignored install failures; make missing credentials fail startup; verify real local signaling/media readiness; configure development/staging/production for LiveKit Cloud; and remove the current LiveKit Fargate Spot service without introducing another self-host cloud topology.
- [ ] 1.37 Replace the LMCache best-effort package install, `metrics_app.py`, and binary-missing fallback with a pinned upstream standalone multiprocess runtime and its real health/metrics endpoints; keep `LMCACHE_ENABLED=false` and desired count zero by default.
- [ ] 1.38 Implement authorized staging-only LMCache GPU integration and workload benchmarking; enable LMCache only after recorded evidence meets the approved latency, throughput, or GPU-cost threshold, and colocate it with vLLM on compatible GPU/IPC topology instead of an independent ARM CPU service.
- [ ] 1.39 Use official Postgres and Redis images for local development and ephemeral smoke tests; keep backend SQL under `backend/db/sql/`; keep managed data resources off by default in dev; and use Terraform-managed RDS and ElastiCache with public-Internet denial, security-group isolation, authentication, and encryption for staging and production.
- [ ] 1.40 Configure the initial ElastiCache deployment for backend sessions only while LiveKit Cloud owns media-plane persistence; document the SLO, paid-load, capacity, availability, or noisy-neighbor evidence that would trigger Multi-AZ or a split.
- [ ] 1.41 Implement platform-specific CI for config validation, ShellCheck/Hadolint where applicable, immutable pin verification, container or official-image vulnerability scanning, and real process smoke checks; exclude routine GPU jobs and product-service typecheck/coverage from platform CI.
- [ ] 1.42 Rename `frontend/` to `workbench/` and scaffold the exact Vite, vanilla TypeScript, Tailwind CSS, ESLint, Vitest, and Playwright layout with one `index.html`, flat responsibility modules, `fixtures.ts`, package lock, configs, scripts, fixtures, and tests; add no UI framework or application server.
- [ ] 1.43 Decompose `stage2.html` into `main.ts`, `api.ts`, `api_types.ts`, `websocket.ts`, `state.ts`, `sessions.ts`, `resources.ts`, `diagnostics.ts`, `livekit.ts`, `simulator.ts`, and `styles.css` while preserving behavior and replacing static substring assertions with module and browser behavior coverage.
- [ ] 1.44 Migrate every Workbench call to canonical `/api/v1` REST and WebSocket contracts, verify direct LiveKit browser media, then remove `lite.html`, superseded static entrypoints, `/lite/*`, `/api/v1/debug/*`, mock routes, and sandbox-test routes from the target tree and production route table.
- [ ] 1.45 Split existing mock data into versioned `workbench/src/fixtures/shop_profiles.json`, `viewer_messages.json`, and `products.json`; include reusable shop/host/persona data, categorized viewer scenarios, and canonical product records; add fail-loud field-level fixture validation and verify no real or secret data is committed.
- [ ] 1.46 Add the exact viewer and admin public fixtures to `workbench/src/dev_tokens.ts`, prefill the local console, configure the local runtime matrix to use them, keep them in page memory only, and make backend startup reject either value outside `APP_ENV=dev|test` without logging it.
- [ ] 1.47 Add a commented, exact-value and exact-path Gitleaks allowlist for only the two public fixtures in `workbench/src/dev_tokens.ts`; test that any other token-like Workbench value still fails secret scanning.
- [ ] 1.48 Move deterministic viewer simulation into `workbench/src/simulator.ts` and black-box smoke behavior into `workbench/scripts/smoke_test.py`; remove `core/debug/` after parity tests prove neither tool imports backend internals or requires a debug API.
- [ ] 1.49 Add Workbench unit and Playwright coverage for API parsing, state transitions, fixture validation, deterministic viewer ingress, token prefill/non-persistence, session configuration, diagnostics, LiveKit connection, interrupt, and stop; run format, lint, typecheck, tests, build, and E2E only when Workbench or a consumed contract changes, and exclude Workbench from backend/production images.
- [ ] 1.50 Classify every existing `core/tests/` case by owner and behavior, migrate product-service unit/integration/contract tests beside their service, move cross-service behavior to root `tests/e2e/` or explicit `tests/sandbox/`, and move Terraform checks to `infra/tests/`.
- [ ] 1.51 Remove collection-time global environment mutation from the legacy test `conftest.py`; construct isolated apps from explicit test settings and inject fake clients or stores only at real system boundaries.
- [ ] 1.52 Add the confirmed backend, LLM, TTS, and avatar test inventories; consolidate tests by behavior rather than preserving a one-to-one migration of fragmented legacy files.
- [ ] 1.53 Generate and commit backend `contracts/v1/{openapi.json,websocket/control.schema.json,websocket/platform.schema.json}` and each model service's `contracts/v1/openapi.json`; create no root `contracts/` registry.
- [ ] 1.54 Add deterministic contract regeneration and drift checks, then map each service-owned artifact change to only its owner and known backend or Workbench consumers.
- [ ] 1.55 Split the current shared runtime project and lock into independent product-service `pyproject.toml` and `uv.lock` files; retain a non-package root `pyproject.toml` and `uv.lock` for cross-service E2E, sandbox, contract-generation, and repository tools only; and keep shared source policy in `.editorconfig`, `ruff.toml`, and `pyrightconfig.json` rather than a shared uv workspace lock.
- [ ] 1.56 Keep repository root as the container build context and add an adjacent `Dockerfile.dockerignore` for every built product or platform Dockerfile; retain root `.dockerignore` only as a conservative fallback.
- [ ] 1.57 Expand one root `.gitignore` for Python, Node, coverage, Playwright, runtime-log, model, secret, and Terraform outputs without ignoring service locks, Workbench locks/fixtures, contract artifacts, Terraform dependency locks, or Gitleaks configuration; add no per-service `.gitignore`.
- [ ] 1.58 Move clustering, Stage 2, and API performance programs plus committed baselines under `benchmarks/`; move `staging_smoke.ps1`, teardown verification, and task-image swapping under `infra/scripts/`; keep service-local and Workbench-local scripts with their owner; retain only affected-area, contract, and shared model-asset helpers under root `scripts/`.
- [ ] 1.59 Enforce the approved test matrix: affected unit tests on feature pushes; affected unit, integration, contract, branch coverage at 80 percent, and cached container validation on pull requests and merge commits; affected cross-service E2E for release verification; and explicit-only sandbox, hosted-provider, GPU, and benchmark jobs.
- [ ] 1.60 Preserve the existing `network`, `security`, `compute`, `database`, `loadbalancer`, `storage`, `secrets`, and `monitoring` Terraform module boundaries; add no service-named Terraform modules.
- [ ] 1.61 Split `infra/modules/compute/main.tf` into `locals.tf`, `cluster.tf`, `iam.tf`, `discovery.tf`, `backend.tf`, `llm.tf`, `tts.tf`, and `avatar.tf` while retaining existing resource addresses for resources whose identity does not change.
- [ ] 1.62 Add `infra/environments/staging/` with its own backend, providers, variables, outputs, example tfvars, dependency lock, and remote state key; keep `global`, `dev`, `staging`, and `prod` isolated without Terraform workspaces.
- [ ] 1.63 Separate the combined LLM/TTS task into independently deployable LLM and TTS task definitions, ECS services, capacity providers, desired counts, health checks, and rollback state; keep avatar independent and backend on Fargate.
- [ ] 1.64 Remove the standalone LMCache ASG/service and integrate the disabled-by-default, evidence-gated runtime only as a colocated LLM topology.
- [ ] 1.65 Remove the LiveKit Fargate Spot service and configure development/staging/production clients for LiveKit Cloud while retaining pinned local/sandbox LiveKit platform assets.
- [ ] 1.66 Remove the internal model-service NLB and configure authenticated backend-to-model calls through environment-specific Cloud Map private DNS; retain only the backend public ALB.
- [ ] 1.67 Implement the canonical runtime matrix: optional local dependencies; manual dev with one Spot backend and managed data/GPU capacity off by default; on-demand staging with managed data; and two on-demand production backend tasks with conditional self-host capacity.
- [ ] 1.68 Add `create_rds` and `create_redis` controls, use memory sessions by default in dev, and configure staging/production for managed Redis sessions while preserving the initial documented single-AZ/single-node MVP ceiling.
- [ ] 1.69 Enforce backend `*_ADAPTER` ownership, service-local `*_ENGINE` ownership, conditional zero desired count/minimum capacity for hosted adapters, and startup rejection of ambiguous legacy selector values.
- [ ] 1.70 Replace mutable image tags in every environment and deployment path with exact digests and add ECS health checks, deployment circuit breakers, affected-service rollback, and temporary replacement capacity where a rolling GPU deployment requires it.
- [ ] 1.71 Run additive raw-SQL runtime migrations once through an explicit pre-deploy backend task and verify the previous backend revision remains compatible after migration.
- [ ] 1.72 Separate S3 state keys, enable versioning/encryption/public blocking/native lockfiles, migrate safely away from DynamoDB locking, and scope state access to the target environment.
- [ ] 1.73 Restrict GitHub OIDC trust to the exact repository and protected dev/staging/production environments; split plan and environment deployment roles and narrowly scope `iam:PassRole`, ECS, SSM, and state permissions.
- [ ] 1.74 Remove plaintext application/provider/API token inputs from Terraform; provision those SSM SecureString values from protected GitHub Environment secrets and pass only parameter ARNs to ECS while documenting the protected-state exception for the no-Secrets-Manager RDS bootstrap password.
- [ ] 1.75 Add Terraform native tests for module invariants and the dev/staging/prod runtime matrix, including zero-cost hosted-adapter topology, no production Spot backend, no LiveKit Fargate/standalone LMCache/internal model NLB, and environment-state isolation.
- [ ] 1.76 Add one root `compose.yaml` with local-only `data` and `media` profiles for official Postgres/Redis and the pinned local LiveKit wrapper; verify it contains no staging/production topology, cloud credentials, or default GPU workload.
- [ ] 1.77 Record Director characterization fixtures and baseline benchmarks before extraction; perform session-context, event/persistence, decision-preparation, playback, queue/chunking, and stream-analysis moves as separate test-gated batches without algorithm changes.
- [ ] 1.78 Move generated runtime data, logs, model artifacts, and benchmark output under `.runtime/`; preserve committed benchmark baselines under `benchmarks/baselines/`; classify research notes and active plans before moving or removing any material file.
- [ ] 1.79 Perform a final reference and route audit before deleting `core/`, `frontend/`, `providers/`, legacy service directories, old workflows, mutable image references, or superseded infrastructure; require target-path tests plus non-production rollback evidence for each cleanup batch.

## 2. Establish Workflow Contracts

- [ ] 2.1 Inventory existing `.github/workflows/` triggers and map each current behavior to the approved event-to-action matrix.
- [ ] 2.2 Define validated workflow inputs for immutable commit SHA, target environment, and supported service identifiers.
- [ ] 2.3 Add static workflow validation that rejects unsupported triggers, invalid reusable-workflow references, and malformed service tag patterns.
- [ ] 2.4 Define repository-aware affected-area rules for product services, platform runtimes, workbench, infrastructure, service-owned contract artifacts, shared configuration, locks, and build files, including consumer-specific fan-out from each contract change.

## 3. Implement Branch-Governed CI

- [ ] 3.1 Refactor `ci.yml` into the always-started event entry point for `feature/*`, `develop`, and `main` pushes and pull requests into `develop` and `main`, and derive fast, integration, or release mode from the trusted event context.
- [ ] 3.2 Add underscore-prefixed `workflow_call` building blocks needed by `ci.yml` without adding deployment triggers to them.
- [ ] 3.3 Ensure the integration gate reports one stable required-check result for branch protection even when unchanged services are skipped.
- [ ] 3.4 Configure and verify `develop` and `main` rulesets requiring pull requests, the stable `CI / gate`, at least one approval, resolved conversations, a current conflict-free head, and denial of direct feature-to-`main` or protected-branch pushes outside audited emergency bypass.
- [ ] 3.5 Add a mandatory Gitleaks `secret-scan` job to `ci.yml` for every push and pull request, with redacted output and a failing exit status on findings.
- [ ] 3.6 Run an initial redacted repository scan, remediate real findings, and add only narrowly scoped reviewed allowlist entries for confirmed false positives.
- [ ] 3.7 Enable and verify GitHub Secret Scanning and Push Protection when supported without weakening the Gitleaks required check.
- [ ] 3.8 Configure `_container-build.yml` to use Docker Buildx with `type=gha` cache import and `mode=max` export under a stable per-service scope.
- [ ] 3.9 Build pull-request images with `push: false`, allow cache export only from trusted runs, and push images only during development or staging deployment builds.
- [ ] 3.10 Verify cold and warm builds for multiple services, cache isolation, and production promotion without rebuilding the staging digest.
- [ ] 3.11 Implement and verify feature-push fast mode: secret scan, affected-area detection, format, lint, typecheck, and unit tests for changed product services, with no integration, contract, coverage, full-container, or deployment job.
- [ ] 3.12 Implement and verify feature-PR integration mode: affected format, lint, typecheck, unit, integration, contract, coverage, cached container build with `push: false`, and conditional workbench, platform, and Terraform checks.
- [ ] 3.13 Implement and verify `develop` merge mode reruns full affected-area integration CI and cached affected-image validation with `push: false` against the exact merge commit.
- [ ] 3.14 Implement and verify `develop`-to-`main` release PR mode and `main` merge verification, including conditional affected service, container, workbench, platform, and Terraform checks without image push or deployment.

## 4. Implement Explicit Development and Staging Deployment

- [ ] 4.1 Implement `deploy-dev.yml` with `workflow_dispatch`, commit/service validation, selected-service cached build/push, digest recording, deployment, smoke verification, and service-scoped rollback.
- [ ] 4.2 Implement `deploy-staging.yml` with `workflow_dispatch`, `main` ancestry and CI validation, selected-service cached build/push, deployment, smoke and E2E verification, and production-eligible digest recording.
- [ ] 4.3 Add terminal wrappers or documented `gh workflow run` commands so development and staging deployment require no GitHub web-console interaction.
- [ ] 4.4 Record deployment evidence including environment, commit, service, initiator, previous digest, new digest, and result.
- [ ] 4.5 Implement protected `infra-apply.yml` as a manual exact-commit workflow that creates, reviews, and applies one saved plan independently of every service deployment workflow.
- [ ] 4.6 Implement protected `infra-teardown-nonprod.yml` with a hard `dev|staging` allowlist, typed confirmation, approval, destroy-plan review, and an impossible production path.

## 5. Implement Service-Scoped Production Release

- [ ] 5.1 Implement `release-service.yml` tag parsing for supported service-specific semantic-version tags.
- [ ] 5.2 Reject release tags whose commit is not contained in `main` or whose service and commit lack successful staging evidence.
- [ ] 5.3 Configure the protected `production` environment to require an authorized approval before secrets or deployment, prevent self-approval and bypass where supported, and fail readiness when approval enforcement is unavailable.
- [ ] 5.4 Promote the exact staging-verified image digest to production without rebuilding the image after approval.
- [ ] 5.5 Run production smoke verification and restore only the affected service's previous digest on failure.

## 6. Verify and Migrate

- [ ] 6.1 Test the feature push, feature-to-develop pull request, develop merge, develop-to-main pull request, and main merge paths and verify that none deploy an environment.
- [ ] 6.2 Test development and staging dispatch through GitHub CLI and verify equivalent input handling through the GitHub web interface or API.
- [ ] 6.3 Test eligible and ineligible production tags, required approval and rejection/unavailability paths, exact-digest promotion, evidence recording, and service-scoped rollback.
- [ ] 6.4 Disable superseded workflow triggers only after the replacement paths pass dry-run and non-production verification.
- [ ] 6.5 Audit the final workflow graph and rulesets against the event-to-job matrix, affected-area fan-out, stable gate behavior, review/conversation/conflict requirements, and absence of implicit deployment.
- [ ] 6.6 Validate `global`, `dev`, `staging`, and `prod` with recursive format, backend-free initialization, validation, native tests, trusted plans, non-production apply/teardown drills, immutable-digest rollout, circuit-breaker rollback, Cloud Map discovery, and secret/state inspection before production infrastructure authorization.
