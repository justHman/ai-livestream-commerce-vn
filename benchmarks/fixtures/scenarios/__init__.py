"""Fixture scenario builders for the canonical Director (task 1.77).

Each scenario exposes ``scenario()`` returning ``{"inputs": ..., "outputs": ...}``
with JSON-safe values; the generator runs them against the legacy
canonical director implementation.
"""
